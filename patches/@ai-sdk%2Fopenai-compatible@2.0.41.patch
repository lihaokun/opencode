diff --git a/dist/index.js b/dist/index.js
--- a/dist/index.js
+++ b/dist/index.js
@@ -755,7 +755,7 @@ var OpenAICompatibleChatLanguageModel = class {
                 delta: delta.content
               });
             }
-            if (delta.tool_calls != null) {
+            if (delta.tool_calls != null && delta.tool_calls.length > 0) {
               if (isActiveReasoning) {
                 controller.enqueue({
                   type: "reasoning-end",
diff --git a/dist/index.mjs b/dist/index.mjs
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -742,7 +742,7 @@ var OpenAICompatibleChatLanguageModel = class {
                 delta: delta.content
               });
             }
-            if (delta.tool_calls != null) {
+            if (delta.tool_calls != null && delta.tool_calls.length > 0) {
               if (isActiveReasoning) {
                 controller.enqueue({
                   type: "reasoning-end",
diff --git a/src/chat/openai-compatible-chat-language-model.ts b/src/chat/openai-compatible-chat-language-model.ts
--- a/src/chat/openai-compatible-chat-language-model.ts
+++ b/src/chat/openai-compatible-chat-language-model.ts
@@ -519,7 +519,7 @@ export class OpenAICompatibleChatLanguageModel
               });
             }

-            if (delta.tool_calls != null) {
+            if (delta.tool_calls != null && delta.tool_calls.length > 0) {
               // end active reasoning block before tool calls start
               if (isActiveReasoning) {
                 controller.enqueue({
